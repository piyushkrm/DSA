
// Ternary Statement
#include <iostream>
using namespace std;

int main() {
    int number;
    cout << "Enter number  : ";
    cin >> number;

    cout << (number > 18 ? "Valid" : "Invalid");
    
    return 0;
    
}